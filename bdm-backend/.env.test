# .env.test - Test Environment Configuration

# Server Config
NODE_ENV=test
PORT=5001

# Database Config - Use separate test database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Vidyavardhaka@1
DB_NAME=bdm_system_test

# JWT Config
JWT_SECRET=test_jwt_secret_key_12345
JWT_EXPIRES_IN=1h

# AI Config (use mock/test keys or disable)
OPENAI_API_KEY=test_key_or_mock
GEMINI_API_KEY=test_key_or_mock

# Disable AI in tests if not needed
ENABLE_AI=false

# Logging
LOG_LEVEL=error